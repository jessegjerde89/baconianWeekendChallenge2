- user can input two values (2 input elements
- select type of mathematical operation
- submit, = button is clicked
- bundle it up in an object,
- send this object to the server via a POST
- 'C' button that will clear the user input fields.

- The server should be able to handle Addition, Subtraction, Multiplication, and Division
- send answer back to the client in an object
- should be displayed on the DOM.

- Keep a historical record on the server
- Display history using a GET request
- Update the list when a new calculation is made.